server:
  port: 8082
spring:
  config:
    name: nacos-config-dem

  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848

management:
  endpoints:
    jmx:
      exposure:
        include: '*'
